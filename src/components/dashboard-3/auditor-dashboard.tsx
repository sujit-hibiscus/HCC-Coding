"use client";

// This is a placeholder component - we're using AnalystDashboard for both roles
// as per the requirements, but we could create a separate dashboard here if needed

import { AnalystDashboard } from "./analyst-dashboard";

export function AuditorDashboard() {
  return <AnalystDashboard />;
}
